services:
  bingo:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3011:3000"
    volumes:
      - ./data:/app/data
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_PATH: /app/data/bingo.sqlite
